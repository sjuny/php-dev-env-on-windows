# AIエージェントのプロジェクト共通処理規約

## 1. 本書の内容

- プロジェクトディレクトリ内でのAIエージェントの処理内容。
- 全ての処理で踏襲、参照する内容。

## 2. 処理の目的

- README.mdに記載されたアプリに関する資料やプログラムコードを生成する。
- コード生成上必要な資料やコードの調査を行う。

## 3. 指示への応答

以下の事項を **必ず** 踏襲して処理する。

- 指示への回答、文書、コードのコメントは日本語にする。
- お礼や謝罪はしない。
- 指示に対しての確認、回答、問題提起、処理結果を断定形で簡潔に述べる。
- ユーザーから指示があれば、**必ず** 処理内容を提示して、承認を得てから実行する。
  - 承認の場合は、yを入力するように促す。
  - 否認の場合は、修正内容を入力するように促す。

## 4. 処理とスキルの関係

- `プログラム作成`
  - 以下のディレクトリ内に格納した仕様書や指示書に従いプログラムコードを作成、または、修正する。
    - docs、.tasks
  - 利用するスキルは以下。
    - $programming

- `プログラム仕様の調査`
  - ユーザーからシステムについての質問があれば、仕様書やソースを参照してレポートを作成する。
  - 利用するスキルは以下。
    - $product-researching

## 5. プロジェクトディレクトリ構成

- プロジェクトディレクトリ内の構成は、README.mdの「プロジェクトディレクトリ構成」セクションに記載。

## 6. パスの指定

### 6-a. プロジェクトルートパス

- 指示書、SKILL.mdに記載している`<project-root-path>`は、このプロジェクトディレクトリのルートまでのパスを表す。

- `<project-root-path>` は、gitリポジトリを含む作業ディレクトリのトップレベルディレクトリのパスとする。

### 6-b. スキルディレクトリパス

- `<skill-directory-path>`は、`<project-root-path>/.agents/skills/`内の当該スキルのディレクトリパスを表す。
  - `<agent-name>`は、実行しているAIエージェントの名前が該当する。例えば、codex, cluade, geminiなど。
  - 例.当該スキルが「coding」の場合、`<skill-directory-path>` = `<project-root-path>/.agents/skills/coding`

### 6-c. タスクディレクトリ

- `タスクディレクトリ`は、`<project-root-path>/.task`配下のタイムスタンプと現在のブランチ名が名前についたディレクトリ。
  - 指示内容、作成計画、処理記録、調査報告に関するMarkdownファイルを格納する。

## 7. 文字コードと改行コード

本プロジェクトの処理は、Windowsで実行されるため、ファイル操作を行う場合は、以下の事項を踏襲する。

- パスの取得やファイル操作は **必ず** OS標準の`PowerShell`で行う。
- ファイルやディレクトリのパス、または、テキストファイルの内容を扱う場合、以下の文字コードと改行コードを利用する。
  - PowerShellのコンソール上でのパスや文字の表示：UTF-8、 LF。
  - Markdown、YAMLファイル、TOMLファイル、Textファイル：UTF-8、 LF。
  - CSVファイル：BOM付きUTF-8、CRLF。値は全て二重引用符で囲む。
